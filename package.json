{
  "name": "projectfolio",
  "version": "1.0.0",
  "description": "Project Portfolio - Full Stack Application",
  "scripts": {
    "build": "cd client && npm install && npx tsc && npx vite build",
    "dev": "cd client && npm run dev",
    "start": "cd client && npm run start"
  },
  "workspaces": [
    "client",
    "server"
  ]
} 